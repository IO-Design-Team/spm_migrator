name: spm_migrator
description: Easily migrate a Flutter plugin to support Swift Package Manager
version: 1.0.0

environment:
  sdk: ^3.11.0

dependencies:
  ansicolor: ^2.0.3
  json_annotation: ^4.10.0
  meta: ^1.18.1
  path: ^1.9.1
  pubspec_parse: ^1.5.0

dev_dependencies:
  rexios_lints: ^17.0.3
  test: ^1.29.0
  build_runner: ^2.11.1
  json_serializable: ^6.12.0

executables:
  spm_migrator:
