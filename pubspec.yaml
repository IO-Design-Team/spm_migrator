name: plugin_swiftpm_migrate
description: A starting point for Dart libraries or applications.
version: 1.0.0

environment:
  sdk: ^3.11.0

dependencies:
dev_dependencies:
  rexios_lints: ^17.0.3
  test: ^1.25.6
