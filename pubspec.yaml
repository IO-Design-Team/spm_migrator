name: spm_migrator
description: Easily migrate a Flutter plugin to support Swift Package Manager
version: 1.0.0

environment:
  sdk: ^3.11.0

dependencies:
  path: ^1.9.1
  pubspec_parse: ^1.5.0

dev_dependencies:
  rexios_lints: ^17.0.3
  test: ^1.25.6
